<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_gallery.xml — Image Grid screen
    Shows all images in the selected folder as a 3-column grid.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorSurface">

    <!-- Top bar: folder path + change folder button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp"
        android:background="?attr/colorPrimary">

        <TextView
            android:id="@+id/tvFolderPath"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="📂 Loading..."
            android:textSize="12sp"
            android:textColor="#FFFFFF"
            android:ellipsize="middle"
            android:maxLines="2"
            android:layout_marginEnd="8dp"/>

        <Button
            android:id="@+id/btnChangeFolder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Change Folder"
            android:textSize="12sp"
            android:textColor="?attr/colorPrimary"
            android:backgroundTint="#FFFFFF"
            style="@style/Widget.MaterialComponents.Button"/>

    </LinearLayout>

    <!-- Empty state message (shown when no images) -->
    <TextView
        android:id="@+id/tvEmptyMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🖼️ No images found in this folder."
        android:textSize="16sp"
        android:textColor="?android:attr/textColorSecondary"
        android:gravity="center"
        android:padding="40dp"
        android:visibility="gone"/>

    <!-- Image Grid -->
    <GridView
        android:id="@+id/gridView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:numColumns="3"
        android:horizontalSpacing="2dp"
        android:verticalSpacing="2dp"
        android:stretchMode="columnWidth"
        android:padding="2dp"/>

</LinearLayout>
